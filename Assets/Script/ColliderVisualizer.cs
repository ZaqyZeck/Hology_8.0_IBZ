using UnityEngine;

public class ColliderVisualizer : MonoBehaviour
{
    private void OnDrawGizmos()
    {
        //GameObject cube = GameObject.CreatePrimitive(PrimitiveType.Cube);
        //cube.transform.SetParent(transform);
        //cube.transform.localPosition = Vector3.zero;
        //cube.transform.localScale = GetComponent<BoxCollider>().size;
        //Material mat = cube.GetComponent<Renderer>().material;
        //mat.color = new Color(0f, 1f, 0f, 0.2f); // green transparent
        //Destroy(cube.GetComponent<Collider>()); // remove extra collider

    }
}
